[project]
name = "studylog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Lbangayan", email = "141669722+Lbangayan@users.noreply.github.com" }
]
requires-python = ">=3.10"
dependencies = [
    "pywin32>=312; sys_platform == 'win32'",
    "rich>=15.0.0",
    "typer>=0.26.7",
]

[project.scripts]
studylog = "studylog:main"

[build-system]
requires = ["uv_build>=0.11.11,<0.12.0"]
build-backend = "uv_build"
